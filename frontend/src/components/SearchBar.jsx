import React from 'react';

const SearchBar = () => (
  <input
    type="text"
    placeholder="Search product..."
    style={{
      padding: '10px',
      width: '100%',
      marginBottom: '1rem',
      fontSize: '16px'
    }}
  />
);

export default SearchBar;
